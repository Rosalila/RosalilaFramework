#include "Rectangle.h"

Rectangle::Rectangle(int x, int y, int width, int height, int angle)
{
    this->x = x;
    this->y = y;
    this->width = width;
    this->height = height;
    this->angle = angle;
}
